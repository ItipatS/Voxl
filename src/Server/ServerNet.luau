--!strict
--!native
--!optimize 2
--!nolint LocalShadow
--!nolint LocalUnused
--!nolint FunctionUnused
--#selene: allow(shadowing)
-- File generated by Blink v0.18.8 (https://github.com/1Axen/Blink)
-- This file is not meant to be edited

_G._BLINK = _G._BLINK or {}
if _G._BLINK["BLINK"] then
	error("[Blink]: An instance of blink is already running with the remote scope \"BLINK\". Change the remote scope of either instance to avoid conflicts.")
end
_G._BLINK["BLINK"] = true
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local SerdesFunctions = {}
local BASE_EVENT_NAME = "BLINK"
local Invocations = 0

local SendSize = 64
local SendOffset = 0
local SendCursor = 0
local SendBuffer = buffer.create(64)
local SendInstances = {}

local RecieveCursor = 0
local RecieveBuffer = buffer.create(64)

local RecieveInstances = {}
local RecieveInstanceCursor = 0

local Null = newproxy()

type Entry = {
    value: any,
    next: Entry?
}

type Queue = {
    head: Entry?,
    tail: Entry?
}

type BufferSave = {
    Size: number, 
    Cursor: number, 
    Buffer: buffer, 
    Instances: {Instance}
}

local function Read(Bytes: number)
    local Offset = RecieveCursor
    RecieveCursor += Bytes
    return Offset
end

local function Save(): BufferSave
    return {
        Size = SendSize,
        Cursor = SendCursor,
        Buffer = SendBuffer,
        Instances = SendInstances
    }
end

local function Load(Save: BufferSave?)
    if Save then
        SendSize = Save.Size
        SendCursor = Save.Cursor
        SendOffset = Save.Cursor
        SendBuffer = Save.Buffer
        SendInstances = Save.Instances
        return
    end

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    SendInstances = {}
end

local function Invoke()
    if Invocations == 255 then
        Invocations = 0
    end

    local Invocation = Invocations
    Invocations += 1
    return Invocation
end

local function Allocate(Bytes: number)
    local InUse = (SendCursor + Bytes)
    if InUse > SendSize then
        --> Avoid resizing the buffer for every write
        while InUse > SendSize do
            SendSize *= 1.5
        end

        local Buffer = buffer.create(SendSize)
        buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
        SendBuffer = Buffer
    end

    SendOffset = SendCursor
    SendCursor += Bytes
    
    return SendOffset
end

local function CreateQueue(): Queue
    return {
        head = nil,
        tail = nil
    }
end

local function Pop(queue: Queue): any
    local head = queue.head
    if head == nil then
        return
    end

    queue.head = head.next
    return head.value
end

local function Push(queue: Queue, value: any)
    local entry: Entry = {
        value = value,
        next = nil
    }

    if queue.tail ~= nil then
        queue.tail.next = entry
    end

    queue.tail = entry

    if queue.head == nil then
        queue.head = entry
    end
end

local Calls = table.create(256)

local Events: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local Queue: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local SerdesEvent = "?"
local SerdesThread: thread?
Queue.Reliable[3] = table.create(256)
Queue.Reliable[7] = table.create(256)
Queue.Reliable[9] = table.create(256)
Queue.Reliable[12] = table.create(256)
Queue.Reliable[13] = table.create(256)
Queue.Reliable[14] = table.create(256)
Queue.Reliable[18] = table.create(256)
Queue.Reliable[20] = table.create(256)
Queue.Reliable[21] = table.create(256)
Queue.Reliable[23] = table.create(256)
Queue.Reliable[24] = table.create(256)
Queue.Reliable[25] = table.create(256)
export type ChunkCoord = { cx: number, cz: number }
export type MapChunk = { cx: number, cz: number, raw: number, data: buffer }
export type BlockEdit = { wx: number, y: number, wz: number, block: number }
export type BlockCount = { block: number, count: number }
export type StarState = { id: number, dp: Vector3 }
export type SlotEntry = { slot: number, item: number, count: number }
export type ItemInv = { weight: number, capacity: number, slots: { { slot: number, item: number, count: number } } }
export type MovePair = { from: number, to: number }
export type PlantRequest = { name: string, cx: number, cz: number }
export type PlantResult = { ok: boolean, reason: string }
export type AsteroidState = { id: number, dp: Vector3, vel: Vector3, size: number, shape: number }
export type AsteroidImpact = { dir: Vector3, stun: number }
export type MoteRef = { cx: number, cy: number, cz: number, idx: number }
export type MoteCool = { ref: { cx: number, cy: number, cz: number, idx: number }, remaining: number }
export type StarSlot = { cx: number, cy: number, cz: number }
export type PlayerStatus = { fuel: number }
function SerdesFunctions.WriteEVENT_LoadChunks(Value: { { cx: number, cz: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 0)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 4 bytes
	local ARRAY_START_1 = Allocate(4 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writei16(SendBuffer, OPERATION_OFFSET_0, Item_1.cx)
		-- Allocate 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writei16(SendBuffer, OPERATION_OFFSET_1, Item_1.cz)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_UnloadChunks(Value: { string }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 1)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 2 bytes
	local ARRAY_START_1 = Allocate(2 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		local Length = #Item_1
		-- Allocate 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_0, Length)
		Allocate(Length)
		buffer.writestring(SendBuffer, SendOffset, Item_1, Length)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_MapChunks(Value: { { cx: number, cz: number, raw: number, data: buffer } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 2)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 8 bytes
	local ARRAY_START_1 = Allocate(8 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writei16(SendBuffer, OPERATION_OFFSET_0, Item_1.cx)
		-- Allocate 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writei16(SendBuffer, OPERATION_OFFSET_1, Item_1.cz)
		-- Allocate 2
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_2, Item_1.raw)
		local Length = buffer.len(Item_1.data)
		-- Allocate 2
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_3, Length)
		Allocate(Length)
		buffer.copy(SendBuffer, SendOffset, Item_1.data, 0, Length)
	end
	-- END ARRAY
end

function SerdesFunctions.ReadEVENT_EditBlock(): ({ wx: number, y: number, wz: number, block: number })
	-- Read BLOCK: 13 bytes
	local BLOCK_START = Read(13)
	local Value = {} :: any
	Value.wx = buffer.readi32(RecieveBuffer, BLOCK_START + 0)
	Value.y = buffer.readi32(RecieveBuffer, BLOCK_START + 4)
	Value.wz = buffer.readi32(RecieveBuffer, BLOCK_START + 8)
	Value.block = buffer.readu8(RecieveBuffer, BLOCK_START + 12)
	return Value
end

function SerdesFunctions.WriteEVENT_BlockEdited(Value: { { wx: number, y: number, wz: number, block: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 4)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 13 bytes
	local ARRAY_START_1 = Allocate(13 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writei32(SendBuffer, OPERATION_OFFSET_0, Item_1.wx)
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writei32(SendBuffer, OPERATION_OFFSET_1, Item_1.y)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writei32(SendBuffer, OPERATION_OFFSET_2, Item_1.wz)
		-- Allocate 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_3, Item_1.block)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_SyncDiff(Value: { { wx: number, y: number, wz: number, block: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 5)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 13 bytes
	local ARRAY_START_1 = Allocate(13 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writei32(SendBuffer, OPERATION_OFFSET_0, Item_1.wx)
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writei32(SendBuffer, OPERATION_OFFSET_1, Item_1.y)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writei32(SendBuffer, OPERATION_OFFSET_2, Item_1.wz)
		-- Allocate 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_3, Item_1.block)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_InventorySync(Value: { { block: number, count: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 6)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 5 bytes
	local ARRAY_START_1 = Allocate(5 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 1
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, Item_1.block)
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_1, Item_1.count)
	end
	-- END ARRAY
end

function SerdesFunctions.ReadEVENT_PlaceLock(): (nil)
	return nil
end

function SerdesFunctions.WriteEVENT_StarStream(Value: { { id: number, dp: Vector3 } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 0)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 14 bytes
	local ARRAY_START_1 = Allocate(14 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		local Vector = Item_1.dp
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_3, Vector.Z)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_StarUnload(Value: { number }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 8)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 2 bytes
	local ARRAY_START_1 = Allocate(2 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_0, Item_1)
	end
	-- END ARRAY
end

function SerdesFunctions.ReadEVENT_StarCatch(): (number)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Value = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	return Value
end

function SerdesFunctions.WriteEVENT_StarCaught(Value: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 10)
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Value)
end

function SerdesFunctions.WriteEVENT_ItemSync(Value: { weight: number, capacity: number, slots: { { slot: number, item: number, count: number } } }): ()
	-- Allocate BLOCK: 11 bytes
	local BLOCK_START = Allocate(11)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 11)
	buffer.writeu32(SendBuffer, BLOCK_START + 1, Value.weight)
	buffer.writeu32(SendBuffer, BLOCK_START + 5, Value.capacity)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 9, #Value.slots)
		-- Allocate BLOCK: 7 bytes
	local ARRAY_START_1 = Allocate(7 * #Value.slots)
	for Index = 1, #Value.slots do
		local Item_1 = Value.slots[Index]
		-- Allocate 1
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, Item_1.slot)
		-- Allocate 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, Item_1.item)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_2, Item_1.count)
	end
	-- END ARRAY
end

function SerdesFunctions.ReadEVENT_MoveItem(): ({ from: number, to: number })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Value = {} :: any
	Value.from = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	Value.to = buffer.readu8(RecieveBuffer, BLOCK_START + 1)
	return Value
end

function SerdesFunctions.ReadEVENT_UseSlot(): (number)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	return Value
end

function SerdesFunctions.ReadEVENT_PlantStar(): ({ name: string, cx: number, cz: number })
	-- Read BLOCK: 6 bytes
	local BLOCK_START = Read(6)
	local Value = {} :: any
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	Value.name = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Value.cx = buffer.readi16(RecieveBuffer, BLOCK_START + 2)
	Value.cz = buffer.readi16(RecieveBuffer, BLOCK_START + 4)
	return Value
end

function SerdesFunctions.WriteEVENT_PlantStarResult(Value: { ok: boolean, reason: string }): ()
	-- Allocate BLOCK: 4 bytes
	local BLOCK_START = Allocate(4)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 15)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value.ok and 1 or 0)
	local Length = #Value.reason
	buffer.writeu16(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value.reason, Length)
end

function SerdesFunctions.WriteEVENT_AsteroidStream(Value: { { id: number, dp: Vector3, vel: Vector3, size: number, shape: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 1)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 31 bytes
	local ARRAY_START_1 = Allocate(31 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		local Vector = Item_1.dp
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_3, Vector.Z)
		local Vector = Item_1.vel
		-- Allocate 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_4, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_5, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_6, Vector.Z)
		-- Allocate 4
		local OPERATION_OFFSET_7 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_7, Item_1.size)
		-- Allocate 1
		local OPERATION_OFFSET_8 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_8, Item_1.shape)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_AsteroidUnload(Value: { number }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 16)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 2 bytes
	local ARRAY_START_1 = Allocate(2 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_0, Item_1)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_AsteroidHit(Value: { dir: Vector3, stun: number }): ()
	-- Allocate BLOCK: 17 bytes
	local BLOCK_START = Allocate(17)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 17)
	local Vector = Value.dir
	buffer.writef32(SendBuffer, BLOCK_START + 1, Vector.X)
	buffer.writef32(SendBuffer, BLOCK_START + 5, Vector.Y)
	buffer.writef32(SendBuffer, BLOCK_START + 9, Vector.Z)
	buffer.writef32(SendBuffer, BLOCK_START + 13, Value.stun)
end

function SerdesFunctions.ReadEVENT_DustCollect(): ({ cx: number, cy: number, cz: number, idx: number })
	-- Read BLOCK: 7 bytes
	local BLOCK_START = Read(7)
	local Value = {} :: any
	Value.cx = buffer.readi16(RecieveBuffer, BLOCK_START + 0)
	Value.cy = buffer.readi16(RecieveBuffer, BLOCK_START + 2)
	Value.cz = buffer.readi16(RecieveBuffer, BLOCK_START + 4)
	Value.idx = buffer.readu8(RecieveBuffer, BLOCK_START + 6)
	return Value
end

function SerdesFunctions.WriteEVENT_DustCooldown(Value: { { ref: { cx: number, cy: number, cz: number, idx: number }, remaining: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 19)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 9 bytes
	local ARRAY_START_1 = Allocate(9 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writei16(SendBuffer, OPERATION_OFFSET_0, Item_1.ref.cx)
		-- Allocate 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writei16(SendBuffer, OPERATION_OFFSET_1, Item_1.ref.cy)
		-- Allocate 2
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writei16(SendBuffer, OPERATION_OFFSET_2, Item_1.ref.cz)
		-- Allocate 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_3, Item_1.ref.idx)
		-- Allocate 2
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_4, Item_1.remaining)
	end
	-- END ARRAY
end

function SerdesFunctions.ReadEVENT_EnterHub(): (number)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	return Value
end

function SerdesFunctions.ReadEVENT_EnterStar(): ({ cx: number, cy: number, cz: number })
	-- Read BLOCK: 6 bytes
	local BLOCK_START = Read(6)
	local Value = {} :: any
	Value.cx = buffer.readi16(RecieveBuffer, BLOCK_START + 0)
	Value.cy = buffer.readi16(RecieveBuffer, BLOCK_START + 2)
	Value.cz = buffer.readi16(RecieveBuffer, BLOCK_START + 4)
	return Value
end

function SerdesFunctions.WriteEVENT_EnterDenied(Value: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 22)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value)
end

function SerdesFunctions.WriteEVENT_PlayerStateSync(Value: { fuel: number }): ()
	-- Allocate BLOCK: 5 bytes
	local BLOCK_START = Allocate(5)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 2)
	buffer.writef32(SendBuffer, BLOCK_START + 1, Value.fuel)
end

function SerdesFunctions.ReadEVENT_BoostState(): (boolean)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
	return Value
end

function SerdesFunctions.ReadEVENT_Craft(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

function SerdesFunctions.ReadEVENT_EquipItem(): (number)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Value = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	return Value
end

if not RunService:IsRunning() then
	local NOOP = function() end
	local Returns = table.freeze({
	LoadChunks = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	UnloadChunks = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	MapChunks = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	EditBlock = {
		On = NOOP
	},
	BlockEdited = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	SyncDiff = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	InventorySync = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	PlaceLock = {
		On = NOOP
	},
	StarStream = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	StarUnload = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	StarCatch = {
		On = NOOP
	},
	StarCaught = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	ItemSync = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	MoveItem = {
		On = NOOP
	},
	UseSlot = {
		On = NOOP
	},
	PlantStar = {
		On = NOOP
	},
	PlantStarResult = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	AsteroidStream = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	AsteroidUnload = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	AsteroidHit = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	DustCollect = {
		On = NOOP
	},
	DustCooldown = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	EnterHub = {
		On = NOOP
	},
	EnterStar = {
		On = NOOP
	},
	EnterDenied = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	PlayerStateSync = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	BoostState = {
		On = NOOP
	},
	Craft = {
		On = NOOP
	},
	EquipItem = {
		On = NOOP
	},
	})
	return Returns :: BLINK_EVENTS_SYMBOL
end

if not RunService:IsServer() then
    error("Server network module can only be required from the server.")
end

local Reliable: RemoteEvent = ReplicatedStorage:FindFirstChild(BASE_EVENT_NAME .. "_RELIABLE_REMOTE") :: RemoteEvent
if not Reliable then
    local RemoteEvent = Instance.new("RemoteEvent")
    RemoteEvent.Name = BASE_EVENT_NAME .. "_RELIABLE_REMOTE"
    RemoteEvent.Parent = ReplicatedStorage
    Reliable = RemoteEvent
end

local Unreliable: UnreliableRemoteEvent = ReplicatedStorage:FindFirstChild(BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE") :: UnreliableRemoteEvent
if not Unreliable then
    local UnreliableRemoteEvent = Instance.new("UnreliableRemoteEvent")
    UnreliableRemoteEvent.Name = BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE"
    UnreliableRemoteEvent.Parent = ReplicatedStorage
    Unreliable = UnreliableRemoteEvent
end

local PlayersMap: {[Player]: BufferSave} = {}

Players.PlayerRemoving:Connect(function(Player)
    PlayersMap[Player] = nil
end)

local function StepReplication()
    for Player, Send in PlayersMap do
        if Send.Cursor <= 0 then
            continue
        end

        local Buffer = buffer.create(Send.Cursor)
        buffer.copy(Buffer, 0, Send.Buffer, 0, Send.Cursor)
        Reliable:FireClient(Player, Buffer, Send.Instances)

        Send.Size = 64
        Send.Cursor = 0
        Send.Buffer = buffer.create(64)
        table.clear(Send.Instances)
    end
end
RunService.Heartbeat:Connect(StepReplication)
Reliable.OnServerEvent:Connect(function(Player: Player, Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 3 then
			local Value: { wx: number, y: number, wz: number, block: number } = SerdesFunctions.ReadEVENT_EditBlock()
			if Events.Reliable[3] ~= nil then
				SerdesEvent = "EditBlock"
				Events.Reliable[3](Player, Value)
			else
				if #Queue.Reliable[3] > 256 then
					warn("[Blink]: Event queue of \"EditBlock\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[3], {Player, Value} :: {any})
			end
		elseif Index == 7 then
			local Value: nil = SerdesFunctions.ReadEVENT_PlaceLock()
			if Events.Reliable[7] ~= nil then
				SerdesEvent = "PlaceLock"
				Events.Reliable[7](Player, Value)
			else
				if #Queue.Reliable[7] > 256 then
					warn("[Blink]: Event queue of \"PlaceLock\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[7], {Player, Value} :: {any})
			end
		elseif Index == 9 then
			local Value: number = SerdesFunctions.ReadEVENT_StarCatch()
			if Events.Reliable[9] ~= nil then
				SerdesEvent = "StarCatch"
				Events.Reliable[9](Player, Value)
			else
				if #Queue.Reliable[9] > 256 then
					warn("[Blink]: Event queue of \"StarCatch\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[9], {Player, Value} :: {any})
			end
		elseif Index == 12 then
			local Value: { from: number, to: number } = SerdesFunctions.ReadEVENT_MoveItem()
			if Events.Reliable[12] ~= nil then
				SerdesEvent = "MoveItem"
				Events.Reliable[12](Player, Value)
			else
				if #Queue.Reliable[12] > 256 then
					warn("[Blink]: Event queue of \"MoveItem\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[12], {Player, Value} :: {any})
			end
		elseif Index == 13 then
			local Value: number = SerdesFunctions.ReadEVENT_UseSlot()
			if Events.Reliable[13] ~= nil then
				SerdesEvent = "UseSlot"
				Events.Reliable[13](Player, Value)
			else
				if #Queue.Reliable[13] > 256 then
					warn("[Blink]: Event queue of \"UseSlot\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[13], {Player, Value} :: {any})
			end
		elseif Index == 14 then
			local Value: { name: string, cx: number, cz: number } = SerdesFunctions.ReadEVENT_PlantStar()
			if Events.Reliable[14] ~= nil then
				SerdesEvent = "PlantStar"
				Events.Reliable[14](Player, Value)
			else
				if #Queue.Reliable[14] > 256 then
					warn("[Blink]: Event queue of \"PlantStar\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[14], {Player, Value} :: {any})
			end
		elseif Index == 18 then
			local Value: { cx: number, cy: number, cz: number, idx: number } = SerdesFunctions.ReadEVENT_DustCollect()
			if Events.Reliable[18] ~= nil then
				SerdesEvent = "DustCollect"
				Events.Reliable[18](Player, Value)
			else
				if #Queue.Reliable[18] > 256 then
					warn("[Blink]: Event queue of \"DustCollect\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[18], {Player, Value} :: {any})
			end
		elseif Index == 20 then
			local Value: number = SerdesFunctions.ReadEVENT_EnterHub()
			if Events.Reliable[20] ~= nil then
				SerdesEvent = "EnterHub"
				Events.Reliable[20](Player, Value)
			else
				if #Queue.Reliable[20] > 256 then
					warn("[Blink]: Event queue of \"EnterHub\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[20], {Player, Value} :: {any})
			end
		elseif Index == 21 then
			local Value: { cx: number, cy: number, cz: number } = SerdesFunctions.ReadEVENT_EnterStar()
			if Events.Reliable[21] ~= nil then
				SerdesEvent = "EnterStar"
				Events.Reliable[21](Player, Value)
			else
				if #Queue.Reliable[21] > 256 then
					warn("[Blink]: Event queue of \"EnterStar\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[21], {Player, Value} :: {any})
			end
		elseif Index == 23 then
			local Value: boolean = SerdesFunctions.ReadEVENT_BoostState()
			if Events.Reliable[23] ~= nil then
				SerdesEvent = "BoostState"
				Events.Reliable[23](Player, Value)
			else
				if #Queue.Reliable[23] > 256 then
					warn("[Blink]: Event queue of \"BoostState\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[23], {Player, Value} :: {any})
			end
		elseif Index == 24 then
			local Value: string = SerdesFunctions.ReadEVENT_Craft()
			if Events.Reliable[24] ~= nil then
				SerdesEvent = "Craft"
				Events.Reliable[24](Player, Value)
			else
				if #Queue.Reliable[24] > 256 then
					warn("[Blink]: Event queue of \"Craft\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[24], {Player, Value} :: {any})
			end
		elseif Index == 25 then
			local Value: number = SerdesFunctions.ReadEVENT_EquipItem()
			if Events.Reliable[25] ~= nil then
				SerdesEvent = "EquipItem"
				Events.Reliable[25](Player, Value)
			else
				if #Queue.Reliable[25] > 256 then
					warn("[Blink]: Event queue of \"EquipItem\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[25], {Player, Value} :: {any})
			end
		end
	end
end)
Unreliable.OnServerEvent:Connect(function(Player: Player, Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	end
end)


local Returns = table.freeze({
	StepReplication = StepReplication,

	LoadChunks = {
		Fire = function(Player: Player, Value: { { cx: number, cz: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_LoadChunks(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { cx: number, cz: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_LoadChunks(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { cx: number, cz: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_LoadChunks(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { cx: number, cz: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_LoadChunks(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	UnloadChunks = {
		Fire = function(Player: Player, Value: { string }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_UnloadChunks(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { string }): ()
			Load()
			SerdesFunctions.WriteEVENT_UnloadChunks(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { string }): ()
			Load()
			SerdesFunctions.WriteEVENT_UnloadChunks(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { string }): ()
			Load()
			SerdesFunctions.WriteEVENT_UnloadChunks(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	MapChunks = {
		Fire = function(Player: Player, Value: { { cx: number, cz: number, raw: number, data: buffer } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_MapChunks(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { cx: number, cz: number, raw: number, data: buffer } }): ()
			Load()
			SerdesFunctions.WriteEVENT_MapChunks(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { cx: number, cz: number, raw: number, data: buffer } }): ()
			Load()
			SerdesFunctions.WriteEVENT_MapChunks(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { cx: number, cz: number, raw: number, data: buffer } }): ()
			Load()
			SerdesFunctions.WriteEVENT_MapChunks(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	EditBlock = {
		On = function(Listener: (Player: Player, Value: { wx: number, y: number, wz: number, block: number }) -> ()): () -> ()
			Events.Reliable[3] = Listener
			for Index, Arguments in Queue.Reliable[3] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[3] = {}
			return function (): ()
				if Events.Reliable[3] == Listener then
					Events.Reliable[3] = nil
				end
			end
		end
	},
	BlockEdited = {
		Fire = function(Player: Player, Value: { { wx: number, y: number, wz: number, block: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_BlockEdited(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { wx: number, y: number, wz: number, block: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_BlockEdited(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { wx: number, y: number, wz: number, block: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_BlockEdited(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { wx: number, y: number, wz: number, block: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_BlockEdited(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	SyncDiff = {
		Fire = function(Player: Player, Value: { { wx: number, y: number, wz: number, block: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_SyncDiff(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { wx: number, y: number, wz: number, block: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_SyncDiff(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { wx: number, y: number, wz: number, block: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_SyncDiff(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { wx: number, y: number, wz: number, block: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_SyncDiff(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	InventorySync = {
		Fire = function(Player: Player, Value: { { block: number, count: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_InventorySync(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { block: number, count: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_InventorySync(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { block: number, count: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_InventorySync(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { block: number, count: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_InventorySync(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	PlaceLock = {
		On = function(Listener: (Player: Player, Value: nil) -> ()): () -> ()
			Events.Reliable[7] = Listener
			for Index, Arguments in Queue.Reliable[7] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[7] = {}
			return function (): ()
				if Events.Reliable[7] == Listener then
					Events.Reliable[7] = nil
				end
			end
		end
	},
	StarStream = {
		Fire = function(Player: Player, Value: { { id: number, dp: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_StarStream(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireClient(Player, Buffer, SendInstances)
		end,
		FireAll = function(Value: { { id: number, dp: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_StarStream(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireAllClients(Buffer, SendInstances)
		end,
		FireList = function(List: {Player}, Value: { { id: number, dp: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_StarStream(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in List do
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, dp: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_StarStream(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
	},
	StarUnload = {
		Fire = function(Player: Player, Value: { number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_StarUnload(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_StarUnload(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_StarUnload(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_StarUnload(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	StarCatch = {
		On = function(Listener: (Player: Player, Value: number) -> ()): () -> ()
			Events.Reliable[9] = Listener
			for Index, Arguments in Queue.Reliable[9] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[9] = {}
			return function (): ()
				if Events.Reliable[9] == Listener then
					Events.Reliable[9] = nil
				end
			end
		end
	},
	StarCaught = {
		Fire = function(Player: Player, Value: number): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_StarCaught(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_StarCaught(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_StarCaught(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_StarCaught(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	ItemSync = {
		Fire = function(Player: Player, Value: { weight: number, capacity: number, slots: { { slot: number, item: number, count: number } } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_ItemSync(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { weight: number, capacity: number, slots: { { slot: number, item: number, count: number } } }): ()
			Load()
			SerdesFunctions.WriteEVENT_ItemSync(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { weight: number, capacity: number, slots: { { slot: number, item: number, count: number } } }): ()
			Load()
			SerdesFunctions.WriteEVENT_ItemSync(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { weight: number, capacity: number, slots: { { slot: number, item: number, count: number } } }): ()
			Load()
			SerdesFunctions.WriteEVENT_ItemSync(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	MoveItem = {
		On = function(Listener: (Player: Player, Value: { from: number, to: number }) -> ()): () -> ()
			Events.Reliable[12] = Listener
			for Index, Arguments in Queue.Reliable[12] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[12] = {}
			return function (): ()
				if Events.Reliable[12] == Listener then
					Events.Reliable[12] = nil
				end
			end
		end
	},
	UseSlot = {
		On = function(Listener: (Player: Player, Value: number) -> ()): () -> ()
			Events.Reliable[13] = Listener
			for Index, Arguments in Queue.Reliable[13] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[13] = {}
			return function (): ()
				if Events.Reliable[13] == Listener then
					Events.Reliable[13] = nil
				end
			end
		end
	},
	PlantStar = {
		On = function(Listener: (Player: Player, Value: { name: string, cx: number, cz: number }) -> ()): () -> ()
			Events.Reliable[14] = Listener
			for Index, Arguments in Queue.Reliable[14] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[14] = {}
			return function (): ()
				if Events.Reliable[14] == Listener then
					Events.Reliable[14] = nil
				end
			end
		end
	},
	PlantStarResult = {
		Fire = function(Player: Player, Value: { ok: boolean, reason: string }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_PlantStarResult(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { ok: boolean, reason: string }): ()
			Load()
			SerdesFunctions.WriteEVENT_PlantStarResult(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { ok: boolean, reason: string }): ()
			Load()
			SerdesFunctions.WriteEVENT_PlantStarResult(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { ok: boolean, reason: string }): ()
			Load()
			SerdesFunctions.WriteEVENT_PlantStarResult(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	AsteroidStream = {
		Fire = function(Player: Player, Value: { { id: number, dp: Vector3, vel: Vector3, size: number, shape: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_AsteroidStream(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireClient(Player, Buffer, SendInstances)
		end,
		FireAll = function(Value: { { id: number, dp: Vector3, vel: Vector3, size: number, shape: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_AsteroidStream(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireAllClients(Buffer, SendInstances)
		end,
		FireList = function(List: {Player}, Value: { { id: number, dp: Vector3, vel: Vector3, size: number, shape: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_AsteroidStream(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in List do
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, dp: Vector3, vel: Vector3, size: number, shape: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_AsteroidStream(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
	},
	AsteroidUnload = {
		Fire = function(Player: Player, Value: { number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_AsteroidUnload(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_AsteroidUnload(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_AsteroidUnload(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_AsteroidUnload(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	AsteroidHit = {
		Fire = function(Player: Player, Value: { dir: Vector3, stun: number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_AsteroidHit(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { dir: Vector3, stun: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_AsteroidHit(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { dir: Vector3, stun: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_AsteroidHit(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { dir: Vector3, stun: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_AsteroidHit(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	DustCollect = {
		On = function(Listener: (Player: Player, Value: { cx: number, cy: number, cz: number, idx: number }) -> ()): () -> ()
			Events.Reliable[18] = Listener
			for Index, Arguments in Queue.Reliable[18] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[18] = {}
			return function (): ()
				if Events.Reliable[18] == Listener then
					Events.Reliable[18] = nil
				end
			end
		end
	},
	DustCooldown = {
		Fire = function(Player: Player, Value: { { ref: { cx: number, cy: number, cz: number, idx: number }, remaining: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_DustCooldown(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { ref: { cx: number, cy: number, cz: number, idx: number }, remaining: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_DustCooldown(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { ref: { cx: number, cy: number, cz: number, idx: number }, remaining: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_DustCooldown(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { ref: { cx: number, cy: number, cz: number, idx: number }, remaining: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_DustCooldown(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	EnterHub = {
		On = function(Listener: (Player: Player, Value: number) -> ()): () -> ()
			Events.Reliable[20] = Listener
			for Index, Arguments in Queue.Reliable[20] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[20] = {}
			return function (): ()
				if Events.Reliable[20] == Listener then
					Events.Reliable[20] = nil
				end
			end
		end
	},
	EnterStar = {
		On = function(Listener: (Player: Player, Value: { cx: number, cy: number, cz: number }) -> ()): () -> ()
			Events.Reliable[21] = Listener
			for Index, Arguments in Queue.Reliable[21] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[21] = {}
			return function (): ()
				if Events.Reliable[21] == Listener then
					Events.Reliable[21] = nil
				end
			end
		end
	},
	EnterDenied = {
		Fire = function(Player: Player, Value: number): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_EnterDenied(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_EnterDenied(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_EnterDenied(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_EnterDenied(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	PlayerStateSync = {
		Fire = function(Player: Player, Value: { fuel: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_PlayerStateSync(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireClient(Player, Buffer, SendInstances)
		end,
		FireAll = function(Value: { fuel: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_PlayerStateSync(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireAllClients(Buffer, SendInstances)
		end,
		FireList = function(List: {Player}, Value: { fuel: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_PlayerStateSync(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in List do
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
		FireExcept = function(Except: Player, Value: { fuel: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_PlayerStateSync(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
	},
	BoostState = {
		On = function(Listener: (Player: Player, Value: boolean) -> ()): () -> ()
			Events.Reliable[23] = Listener
			for Index, Arguments in Queue.Reliable[23] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[23] = {}
			return function (): ()
				if Events.Reliable[23] == Listener then
					Events.Reliable[23] = nil
				end
			end
		end
	},
	Craft = {
		On = function(Listener: (Player: Player, Value: string) -> ()): () -> ()
			Events.Reliable[24] = Listener
			for Index, Arguments in Queue.Reliable[24] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[24] = {}
			return function (): ()
				if Events.Reliable[24] == Listener then
					Events.Reliable[24] = nil
				end
			end
		end
	},
	EquipItem = {
		On = function(Listener: (Player: Player, Value: number) -> ()): () -> ()
			Events.Reliable[25] = Listener
			for Index, Arguments in Queue.Reliable[25] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[25] = {}
			return function (): ()
				if Events.Reliable[25] == Listener then
					Events.Reliable[25] = nil
				end
			end
		end
	},
})
type BLINK_EVENTS_SYMBOL = typeof(Returns)
return Returns :: BLINK_EVENTS_SYMBOL
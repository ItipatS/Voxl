--!strict
--!native
--!optimize 2
--!nolint LocalShadow
--!nolint LocalUnused
--!nolint FunctionUnused
--#selene: allow(shadowing)
-- File generated by Blink v0.18.8 (https://github.com/1Axen/Blink)
-- This file is not meant to be edited

_G._BLINK = _G._BLINK or {}
if _G._BLINK["BLINK"] then
	error("[Blink]: An instance of blink is already running with the remote scope \"BLINK\". Change the remote scope of either instance to avoid conflicts.")
end
_G._BLINK["BLINK"] = true
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local SerdesFunctions = {}
local BASE_EVENT_NAME = "BLINK"
local Invocations = 0

local SendSize = 64
local SendOffset = 0
local SendCursor = 0
local SendBuffer = buffer.create(64)
local SendInstances = {}

local RecieveCursor = 0
local RecieveBuffer = buffer.create(64)

local RecieveInstances = {}
local RecieveInstanceCursor = 0

local Null = newproxy()

type Entry = {
    value: any,
    next: Entry?
}

type Queue = {
    head: Entry?,
    tail: Entry?
}

type BufferSave = {
    Size: number, 
    Cursor: number, 
    Buffer: buffer, 
    Instances: {Instance}
}

local function Read(Bytes: number)
    local Offset = RecieveCursor
    RecieveCursor += Bytes
    return Offset
end

local function Save(): BufferSave
    return {
        Size = SendSize,
        Cursor = SendCursor,
        Buffer = SendBuffer,
        Instances = SendInstances
    }
end

local function Load(Save: BufferSave?)
    if Save then
        SendSize = Save.Size
        SendCursor = Save.Cursor
        SendOffset = Save.Cursor
        SendBuffer = Save.Buffer
        SendInstances = Save.Instances
        return
    end

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    SendInstances = {}
end

local function Invoke()
    if Invocations == 255 then
        Invocations = 0
    end

    local Invocation = Invocations
    Invocations += 1
    return Invocation
end

local function Allocate(Bytes: number)
    local InUse = (SendCursor + Bytes)
    if InUse > SendSize then
        --> Avoid resizing the buffer for every write
        while InUse > SendSize do
            SendSize *= 1.5
        end

        local Buffer = buffer.create(SendSize)
        buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
        SendBuffer = Buffer
    end

    SendOffset = SendCursor
    SendCursor += Bytes
    
    return SendOffset
end

local function CreateQueue(): Queue
    return {
        head = nil,
        tail = nil
    }
end

local function Pop(queue: Queue): any
    local head = queue.head
    if head == nil then
        return
    end

    queue.head = head.next
    return head.value
end

local function Push(queue: Queue, value: any)
    local entry: Entry = {
        value = value,
        next = nil
    }

    if queue.tail ~= nil then
        queue.tail.next = entry
    end

    queue.tail = entry

    if queue.head == nil then
        queue.head = entry
    end
end

local Calls = table.create(256)

local Events: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local Queue: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local SerdesEvent = "?"
local SerdesThread: thread?
Queue.Reliable[0] = table.create(256)
Queue.Reliable[1] = table.create(256)
Queue.Reliable[3] = table.create(256)
Queue.Reliable[4] = table.create(256)
Queue.Reliable[5] = table.create(256)
Queue.Unreliable[0] = table.create(256)
Queue.Reliable[7] = table.create(256)
Queue.Reliable[9] = table.create(256)
Queue.Reliable[10] = table.create(256)
Queue.Reliable[14] = table.create(256)
Queue.Unreliable[1] = table.create(256)
Queue.Reliable[15] = table.create(256)
Queue.Reliable[16] = table.create(256)
Queue.Reliable[18] = table.create(256)
Queue.Unreliable[2] = table.create(256)
export type ChunkCoord = { cx: number, cz: number }
export type BlockEdit = { wx: number, y: number, wz: number, block: number }
export type BlockCount = { block: number, count: number }
export type StarState = { id: number, dp: Vector3 }
export type SlotEntry = { slot: number, item: number, count: number }
export type ItemInv = { weight: number, capacity: number, slots: { { slot: number, item: number, count: number } } }
export type MovePair = { from: number, to: number }
export type PlantRequest = { name: string, cx: number, cz: number }
export type PlantResult = { ok: boolean, reason: string }
export type AsteroidState = { id: number, dp: Vector3, vel: Vector3, size: number, shape: number }
export type AsteroidImpact = { dir: Vector3, stun: number }
export type MoteRef = { cx: number, cy: number, cz: number, idx: number }
export type MoteCool = { ref: { cx: number, cy: number, cz: number, idx: number }, remaining: number }
export type StarSlot = { cx: number, cy: number, cz: number }
export type PlayerStatus = { fuel: number }
function SerdesFunctions.ReadEVENT_LoadChunks(): ({ { cx: number, cz: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 4 bytes
	local ARRAY_START_1 = Read(4 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		Item_1.cx = buffer.readi16(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		Item_1.cz = buffer.readi16(RecieveBuffer, OPERATION_OFFSET_1)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_UnloadChunks(): ({ string })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 2 bytes
	local ARRAY_START_1 = Read(2 * Length)
	for Index = 1, Length do
		-- Read 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		Length = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
		local Item_1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.WriteEVENT_EditBlock(Value: { wx: number, y: number, wz: number, block: number }): ()
	-- Allocate BLOCK: 14 bytes
	local BLOCK_START = Allocate(14)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 2)
	buffer.writei32(SendBuffer, BLOCK_START + 1, Value.wx)
	buffer.writei32(SendBuffer, BLOCK_START + 5, Value.y)
	buffer.writei32(SendBuffer, BLOCK_START + 9, Value.wz)
	buffer.writeu8(SendBuffer, BLOCK_START + 13, Value.block)
end

function SerdesFunctions.ReadEVENT_BlockEdited(): ({ { wx: number, y: number, wz: number, block: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 13 bytes
	local ARRAY_START_1 = Read(13 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.wx = buffer.readi32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.y = buffer.readi32(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.wz = buffer.readi32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.block = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_3)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_SyncDiff(): ({ { wx: number, y: number, wz: number, block: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 13 bytes
	local ARRAY_START_1 = Read(13 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.wx = buffer.readi32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.y = buffer.readi32(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.wz = buffer.readi32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.block = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_3)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_InventorySync(): ({ { block: number, count: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 5 bytes
	local ARRAY_START_1 = Read(5 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 1
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.block = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.count = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_1)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.WriteEVENT_PlaceLock(Value: nil): ()
	-- Allocate BLOCK: 1 bytes
	local BLOCK_START = Allocate(1)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 6)
end

function SerdesFunctions.ReadEVENT_StarStream(): ({ { id: number, dp: Vector3 } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 14 bytes
	local ARRAY_START_1 = Read(14 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		Item_1.id = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
		Item_1.dp = Vector3.new(X, Y, Z)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_StarUnload(): ({ number })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 2 bytes
	local ARRAY_START_1 = Read(2 * Length)
	for Index = 1, Length do
		-- Read 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		local Item_1 = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.WriteEVENT_StarCatch(Value: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 8)
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Value)
end

function SerdesFunctions.ReadEVENT_StarCaught(): (number)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Value = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	return Value
end

function SerdesFunctions.ReadEVENT_ItemSync(): ({ weight: number, capacity: number, slots: { { slot: number, item: number, count: number } } })
	-- Read BLOCK: 10 bytes
	local BLOCK_START = Read(10)
	local Value = {} :: any
	Value.weight = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
	Value.capacity = buffer.readu32(RecieveBuffer, BLOCK_START + 4)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 8)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	Value.slots = table.create(Length)
		-- Read BLOCK: 7 bytes
	local ARRAY_START_1 = Read(7 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 1
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.slot = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		Item_1.item = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.count = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_2)
		table.insert(Value.slots, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.WriteEVENT_MoveItem(Value: { from: number, to: number }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 11)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value.from)
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Value.to)
end

function SerdesFunctions.WriteEVENT_UseSlot(Value: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 12)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value)
end

function SerdesFunctions.WriteEVENT_PlantStar(Value: { name: string, cx: number, cz: number }): ()
	-- Allocate BLOCK: 7 bytes
	local BLOCK_START = Allocate(7)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 13)
	local Length = #Value.name
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value.name, Length)
	buffer.writei16(SendBuffer, BLOCK_START + 3, Value.cx)
	buffer.writei16(SendBuffer, BLOCK_START + 5, Value.cz)
end

function SerdesFunctions.ReadEVENT_PlantStarResult(): ({ ok: boolean, reason: string })
	-- Read BLOCK: 3 bytes
	local BLOCK_START = Read(3)
	local Value = {} :: any
	Value.ok = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 1)
	Value.reason = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

function SerdesFunctions.ReadEVENT_AsteroidStream(): ({ { id: number, dp: Vector3, vel: Vector3, size: number, shape: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 31 bytes
	local ARRAY_START_1 = Read(31 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		Item_1.id = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
		Item_1.dp = Vector3.new(X, Y, Z)
		-- Read 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_4)
		-- Read 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_5)
		-- Read 4
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 4
		Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_6)
		Item_1.vel = Vector3.new(X, Y, Z)
		-- Read 4
		local OPERATION_OFFSET_7 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.size = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_7)
		-- Read 1
		local OPERATION_OFFSET_8 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.shape = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_8)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_AsteroidUnload(): ({ number })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 2 bytes
	local ARRAY_START_1 = Read(2 * Length)
	for Index = 1, Length do
		-- Read 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		local Item_1 = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_AsteroidHit(): ({ dir: Vector3, stun: number })
	-- Read BLOCK: 16 bytes
	local BLOCK_START = Read(16)
	local Value = {} :: any
	local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
	local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
	local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
	Value.dir = Vector3.new(X, Y, Z)
	Value.stun = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
	return Value
end

function SerdesFunctions.WriteEVENT_DustCollect(Value: { cx: number, cy: number, cz: number, idx: number }): ()
	-- Allocate BLOCK: 8 bytes
	local BLOCK_START = Allocate(8)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 17)
	buffer.writei16(SendBuffer, BLOCK_START + 1, Value.cx)
	buffer.writei16(SendBuffer, BLOCK_START + 3, Value.cy)
	buffer.writei16(SendBuffer, BLOCK_START + 5, Value.cz)
	buffer.writeu8(SendBuffer, BLOCK_START + 7, Value.idx)
end

function SerdesFunctions.ReadEVENT_DustCooldown(): ({ { ref: { cx: number, cy: number, cz: number, idx: number }, remaining: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 9 bytes
	local ARRAY_START_1 = Read(9 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		Item_1.ref = {} :: any
		-- Read 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		Item_1.ref.cx = buffer.readi16(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		Item_1.ref.cy = buffer.readi16(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 2
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 2
		Item_1.ref.cz = buffer.readi16(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.ref.idx = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_3)
		-- Read 2
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 2
		Item_1.remaining = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_4)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.WriteEVENT_EnterStar(Value: { cx: number, cy: number, cz: number }): ()
	-- Allocate BLOCK: 7 bytes
	local BLOCK_START = Allocate(7)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 19)
	buffer.writei16(SendBuffer, BLOCK_START + 1, Value.cx)
	buffer.writei16(SendBuffer, BLOCK_START + 3, Value.cy)
	buffer.writei16(SendBuffer, BLOCK_START + 5, Value.cz)
end

function SerdesFunctions.ReadEVENT_PlayerStateSync(): ({ fuel: number })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Value = {} :: any
	Value.fuel = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
	return Value
end

function SerdesFunctions.WriteEVENT_BoostState(Value: boolean): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 20)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value and 1 or 0)
end

function SerdesFunctions.WriteEVENT_Craft(Value: string): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 21)
	local Length = #Value
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.WriteEVENT_EquipItem(Value: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 22)
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Value)
end

if not RunService:IsRunning() then
	local NOOP = function() end
	local Returns = table.freeze({
	LoadChunks = {
		On = NOOP
	},
	UnloadChunks = {
		On = NOOP
	},
	EditBlock = {
		Fire = NOOP
	},
	BlockEdited = {
		On = NOOP
	},
	SyncDiff = {
		On = NOOP
	},
	InventorySync = {
		On = NOOP
	},
	PlaceLock = {
		Fire = NOOP
	},
	StarStream = {
		On = NOOP
	},
	StarUnload = {
		On = NOOP
	},
	StarCatch = {
		Fire = NOOP
	},
	StarCaught = {
		On = NOOP
	},
	ItemSync = {
		On = NOOP
	},
	MoveItem = {
		Fire = NOOP
	},
	UseSlot = {
		Fire = NOOP
	},
	PlantStar = {
		Fire = NOOP
	},
	PlantStarResult = {
		On = NOOP
	},
	AsteroidStream = {
		On = NOOP
	},
	AsteroidUnload = {
		On = NOOP
	},
	AsteroidHit = {
		On = NOOP
	},
	DustCollect = {
		Fire = NOOP
	},
	DustCooldown = {
		On = NOOP
	},
	EnterStar = {
		Fire = NOOP
	},
	PlayerStateSync = {
		On = NOOP
	},
	BoostState = {
		Fire = NOOP
	},
	Craft = {
		Fire = NOOP
	},
	EquipItem = {
		Fire = NOOP
	},
	})
	return Returns :: BLINK_EVENTS_SYMBOL
end

if not RunService:IsClient() then
    error("Client network module can only be required from the client.")
end

local Reliable: RemoteEvent = ReplicatedStorage:WaitForChild(BASE_EVENT_NAME .. "_RELIABLE_REMOTE") :: RemoteEvent
local Unreliable: UnreliableRemoteEvent = ReplicatedStorage:WaitForChild(BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE") :: UnreliableRemoteEvent

local function StepReplication()
    if SendCursor <= 0 then
        return
    end

    local Buffer = buffer.create(SendCursor)
    buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
    Reliable:FireServer(Buffer, SendInstances)

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    table.clear(SendInstances)
end
RunService.Heartbeat:Connect(StepReplication)
Reliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 0 then
			local Value: { { cx: number, cz: number } } = SerdesFunctions.ReadEVENT_LoadChunks()
			if Events.Reliable[0] ~= nil then
				SerdesEvent = "LoadChunks"
				Events.Reliable[0](Value)
			else
				if #Queue.Reliable[0] > 256 then
					warn("[Blink]: Event queue of \"LoadChunks\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[0], {Value} :: {any})
			end
		elseif Index == 1 then
			local Value: { string } = SerdesFunctions.ReadEVENT_UnloadChunks()
			if Events.Reliable[1] ~= nil then
				SerdesEvent = "UnloadChunks"
				Events.Reliable[1](Value)
			else
				if #Queue.Reliable[1] > 256 then
					warn("[Blink]: Event queue of \"UnloadChunks\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[1], {Value} :: {any})
			end
		elseif Index == 3 then
			local Value: { { wx: number, y: number, wz: number, block: number } } = SerdesFunctions.ReadEVENT_BlockEdited()
			if Events.Reliable[3] ~= nil then
				SerdesEvent = "BlockEdited"
				Events.Reliable[3](Value)
			else
				if #Queue.Reliable[3] > 256 then
					warn("[Blink]: Event queue of \"BlockEdited\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[3], {Value} :: {any})
			end
		elseif Index == 4 then
			local Value: { { wx: number, y: number, wz: number, block: number } } = SerdesFunctions.ReadEVENT_SyncDiff()
			if Events.Reliable[4] ~= nil then
				SerdesEvent = "SyncDiff"
				Events.Reliable[4](Value)
			else
				if #Queue.Reliable[4] > 256 then
					warn("[Blink]: Event queue of \"SyncDiff\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[4], {Value} :: {any})
			end
		elseif Index == 5 then
			local Value: { { block: number, count: number } } = SerdesFunctions.ReadEVENT_InventorySync()
			if Events.Reliable[5] ~= nil then
				SerdesEvent = "InventorySync"
				Events.Reliable[5](Value)
			else
				if #Queue.Reliable[5] > 256 then
					warn("[Blink]: Event queue of \"InventorySync\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[5], {Value} :: {any})
			end
		elseif Index == 7 then
			local Value: { number } = SerdesFunctions.ReadEVENT_StarUnload()
			if Events.Reliable[7] ~= nil then
				SerdesEvent = "StarUnload"
				Events.Reliable[7](Value)
			else
				if #Queue.Reliable[7] > 256 then
					warn("[Blink]: Event queue of \"StarUnload\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[7], {Value} :: {any})
			end
		elseif Index == 9 then
			local Value: number = SerdesFunctions.ReadEVENT_StarCaught()
			if Events.Reliable[9] ~= nil then
				SerdesEvent = "StarCaught"
				Events.Reliable[9](Value)
			else
				if #Queue.Reliable[9] > 256 then
					warn("[Blink]: Event queue of \"StarCaught\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[9], {Value} :: {any})
			end
		elseif Index == 10 then
			local Value: { weight: number, capacity: number, slots: { { slot: number, item: number, count: number } } } = SerdesFunctions.ReadEVENT_ItemSync()
			if Events.Reliable[10] ~= nil then
				SerdesEvent = "ItemSync"
				Events.Reliable[10](Value)
			else
				if #Queue.Reliable[10] > 256 then
					warn("[Blink]: Event queue of \"ItemSync\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[10], {Value} :: {any})
			end
		elseif Index == 14 then
			local Value: { ok: boolean, reason: string } = SerdesFunctions.ReadEVENT_PlantStarResult()
			if Events.Reliable[14] ~= nil then
				SerdesEvent = "PlantStarResult"
				Events.Reliable[14](Value)
			else
				if #Queue.Reliable[14] > 256 then
					warn("[Blink]: Event queue of \"PlantStarResult\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[14], {Value} :: {any})
			end
		elseif Index == 15 then
			local Value: { number } = SerdesFunctions.ReadEVENT_AsteroidUnload()
			if Events.Reliable[15] ~= nil then
				SerdesEvent = "AsteroidUnload"
				Events.Reliable[15](Value)
			else
				if #Queue.Reliable[15] > 256 then
					warn("[Blink]: Event queue of \"AsteroidUnload\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[15], {Value} :: {any})
			end
		elseif Index == 16 then
			local Value: { dir: Vector3, stun: number } = SerdesFunctions.ReadEVENT_AsteroidHit()
			if Events.Reliable[16] ~= nil then
				SerdesEvent = "AsteroidHit"
				Events.Reliable[16](Value)
			else
				if #Queue.Reliable[16] > 256 then
					warn("[Blink]: Event queue of \"AsteroidHit\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[16], {Value} :: {any})
			end
		elseif Index == 18 then
			local Value: { { ref: { cx: number, cy: number, cz: number, idx: number }, remaining: number } } = SerdesFunctions.ReadEVENT_DustCooldown()
			if Events.Reliable[18] ~= nil then
				SerdesEvent = "DustCooldown"
				Events.Reliable[18](Value)
			else
				if #Queue.Reliable[18] > 256 then
					warn("[Blink]: Event queue of \"DustCooldown\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[18], {Value} :: {any})
			end
		end
	end
end)
Unreliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 0 then
			local Value: { { id: number, dp: Vector3 } } = SerdesFunctions.ReadEVENT_StarStream()
			if Events.Unreliable[0] ~= nil then
				SerdesEvent = "StarStream"
				Events.Unreliable[0](Value)
			end
		elseif Index == 1 then
			local Value: { { id: number, dp: Vector3, vel: Vector3, size: number, shape: number } } = SerdesFunctions.ReadEVENT_AsteroidStream()
			if Events.Unreliable[1] ~= nil then
				SerdesEvent = "AsteroidStream"
				Events.Unreliable[1](Value)
			end
		elseif Index == 2 then
			local Value: { fuel: number } = SerdesFunctions.ReadEVENT_PlayerStateSync()
			if Events.Unreliable[2] ~= nil then
				SerdesEvent = "PlayerStateSync"
				Events.Unreliable[2](Value)
			end
		end
	end
end)


local Returns = table.freeze({
	StepReplication = StepReplication,

	LoadChunks = {
		On = function(Listener: (Value: { { cx: number, cz: number } }) -> ()): () -> ()
			Events.Reliable[0] = Listener
			for Index, Arguments in Queue.Reliable[0] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[0] = {}
			return function (): ()
				if Events.Reliable[0] == Listener then
					Events.Reliable[0] = nil
				end
			end
		end
	},
	UnloadChunks = {
		On = function(Listener: (Value: { string }) -> ()): () -> ()
			Events.Reliable[1] = Listener
			for Index, Arguments in Queue.Reliable[1] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[1] = {}
			return function (): ()
				if Events.Reliable[1] == Listener then
					Events.Reliable[1] = nil
				end
			end
		end
	},
	EditBlock = {
		Fire = function(Value: { wx: number, y: number, wz: number, block: number }): ()
			SerdesFunctions.WriteEVENT_EditBlock(Value)
		end
	},
	BlockEdited = {
		On = function(Listener: (Value: { { wx: number, y: number, wz: number, block: number } }) -> ()): () -> ()
			Events.Reliable[3] = Listener
			for Index, Arguments in Queue.Reliable[3] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[3] = {}
			return function (): ()
				if Events.Reliable[3] == Listener then
					Events.Reliable[3] = nil
				end
			end
		end
	},
	SyncDiff = {
		On = function(Listener: (Value: { { wx: number, y: number, wz: number, block: number } }) -> ()): () -> ()
			Events.Reliable[4] = Listener
			for Index, Arguments in Queue.Reliable[4] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[4] = {}
			return function (): ()
				if Events.Reliable[4] == Listener then
					Events.Reliable[4] = nil
				end
			end
		end
	},
	InventorySync = {
		On = function(Listener: (Value: { { block: number, count: number } }) -> ()): () -> ()
			Events.Reliable[5] = Listener
			for Index, Arguments in Queue.Reliable[5] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[5] = {}
			return function (): ()
				if Events.Reliable[5] == Listener then
					Events.Reliable[5] = nil
				end
			end
		end
	},
	PlaceLock = {
		Fire = function(Value: nil): ()
			SerdesFunctions.WriteEVENT_PlaceLock(Value)
		end
	},
	StarStream = {
		On = function(Listener: (Value: { { id: number, dp: Vector3 } }) -> ()): () -> ()
			Events.Unreliable[0] = Listener
			return function (): ()
				if Events.Unreliable[0] == Listener then
					Events.Unreliable[0] = nil
				end
			end
		end
	},
	StarUnload = {
		On = function(Listener: (Value: { number }) -> ()): () -> ()
			Events.Reliable[7] = Listener
			for Index, Arguments in Queue.Reliable[7] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[7] = {}
			return function (): ()
				if Events.Reliable[7] == Listener then
					Events.Reliable[7] = nil
				end
			end
		end
	},
	StarCatch = {
		Fire = function(Value: number): ()
			SerdesFunctions.WriteEVENT_StarCatch(Value)
		end
	},
	StarCaught = {
		On = function(Listener: (Value: number) -> ()): () -> ()
			Events.Reliable[9] = Listener
			for Index, Arguments in Queue.Reliable[9] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[9] = {}
			return function (): ()
				if Events.Reliable[9] == Listener then
					Events.Reliable[9] = nil
				end
			end
		end
	},
	ItemSync = {
		On = function(Listener: (Value: { weight: number, capacity: number, slots: { { slot: number, item: number, count: number } } }) -> ()): () -> ()
			Events.Reliable[10] = Listener
			for Index, Arguments in Queue.Reliable[10] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[10] = {}
			return function (): ()
				if Events.Reliable[10] == Listener then
					Events.Reliable[10] = nil
				end
			end
		end
	},
	MoveItem = {
		Fire = function(Value: { from: number, to: number }): ()
			SerdesFunctions.WriteEVENT_MoveItem(Value)
		end
	},
	UseSlot = {
		Fire = function(Value: number): ()
			SerdesFunctions.WriteEVENT_UseSlot(Value)
		end
	},
	PlantStar = {
		Fire = function(Value: { name: string, cx: number, cz: number }): ()
			SerdesFunctions.WriteEVENT_PlantStar(Value)
		end
	},
	PlantStarResult = {
		On = function(Listener: (Value: { ok: boolean, reason: string }) -> ()): () -> ()
			Events.Reliable[14] = Listener
			for Index, Arguments in Queue.Reliable[14] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[14] = {}
			return function (): ()
				if Events.Reliable[14] == Listener then
					Events.Reliable[14] = nil
				end
			end
		end
	},
	AsteroidStream = {
		On = function(Listener: (Value: { { id: number, dp: Vector3, vel: Vector3, size: number, shape: number } }) -> ()): () -> ()
			Events.Unreliable[1] = Listener
			return function (): ()
				if Events.Unreliable[1] == Listener then
					Events.Unreliable[1] = nil
				end
			end
		end
	},
	AsteroidUnload = {
		On = function(Listener: (Value: { number }) -> ()): () -> ()
			Events.Reliable[15] = Listener
			for Index, Arguments in Queue.Reliable[15] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[15] = {}
			return function (): ()
				if Events.Reliable[15] == Listener then
					Events.Reliable[15] = nil
				end
			end
		end
	},
	AsteroidHit = {
		On = function(Listener: (Value: { dir: Vector3, stun: number }) -> ()): () -> ()
			Events.Reliable[16] = Listener
			for Index, Arguments in Queue.Reliable[16] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[16] = {}
			return function (): ()
				if Events.Reliable[16] == Listener then
					Events.Reliable[16] = nil
				end
			end
		end
	},
	DustCollect = {
		Fire = function(Value: { cx: number, cy: number, cz: number, idx: number }): ()
			SerdesFunctions.WriteEVENT_DustCollect(Value)
		end
	},
	DustCooldown = {
		On = function(Listener: (Value: { { ref: { cx: number, cy: number, cz: number, idx: number }, remaining: number } }) -> ()): () -> ()
			Events.Reliable[18] = Listener
			for Index, Arguments in Queue.Reliable[18] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[18] = {}
			return function (): ()
				if Events.Reliable[18] == Listener then
					Events.Reliable[18] = nil
				end
			end
		end
	},
	EnterStar = {
		Fire = function(Value: { cx: number, cy: number, cz: number }): ()
			SerdesFunctions.WriteEVENT_EnterStar(Value)
		end
	},
	PlayerStateSync = {
		On = function(Listener: (Value: { fuel: number }) -> ()): () -> ()
			Events.Unreliable[2] = Listener
			return function (): ()
				if Events.Unreliable[2] == Listener then
					Events.Unreliable[2] = nil
				end
			end
		end
	},
	BoostState = {
		Fire = function(Value: boolean): ()
			SerdesFunctions.WriteEVENT_BoostState(Value)
		end
	},
	Craft = {
		Fire = function(Value: string): ()
			SerdesFunctions.WriteEVENT_Craft(Value)
		end
	},
	EquipItem = {
		Fire = function(Value: number): ()
			SerdesFunctions.WriteEVENT_EquipItem(Value)
		end
	},
})
type BLINK_EVENTS_SYMBOL = typeof(Returns)
return Returns :: BLINK_EVENTS_SYMBOL